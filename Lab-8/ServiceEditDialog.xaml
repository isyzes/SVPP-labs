<Window x:Class="Lab8.ServiceEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab8"
        mc:Ignorable="d"
        Title="Редактирование услуги" 
        Height="500" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5 5 5 0"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10 5"/>
            <Setter Property="MinWidth" Value="80"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Название -->
        <TextBlock Grid.Row="0" Text="Название услуги:" FontWeight="Bold"/>
        <TextBox Grid.Row="1" x:Name="NameTextBox" MaxLength="100"
                 Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Категория -->
        <TextBlock Grid.Row="2" Text="Категория:" FontWeight="Bold"/>
        <TextBox Grid.Row="3" x:Name="CategoryTextBox" MaxLength="50"
                 Text="{Binding Category, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Описание -->
        <TextBlock Grid.Row="4" Text="Описание:" FontWeight="Bold"/>
        <TextBox Grid.Row="5" x:Name="DescriptionTextBox" 
                 MaxLength="500" Height="60" 
                 TextWrapping="Wrap" AcceptsReturn="True"
                 Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Цена -->
        <TextBlock Grid.Row="6" Text="Цена (руб.):" FontWeight="Bold"/>
        <TextBox Grid.Row="7" x:Name="PriceTextBox"
                 Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Поставщик -->
        <TextBlock Grid.Row="8" Text="Поставщик:" FontWeight="Bold"/>
        <TextBox Grid.Row="9" x:Name="ProviderTextBox" MaxLength="100"
                 Text="{Binding Provider, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Адрес -->
        <TextBlock Grid.Row="10" Text="Адрес:" FontWeight="Bold"/>
        <TextBox Grid.Row="11" x:Name="AddressTextBox" MaxLength="200"
                 Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Телефон -->
        <TextBlock Grid.Row="12" Text="Телефон:" FontWeight="Bold" />
        <TextBox Grid.Row="13" x:Name="PhoneTextBox" MaxLength="20"
                 Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Активность -->
        <CheckBox Grid.Row="15" x:Name="IsActiveCheckBox" 
                  Content="Услуга активна" 
                  Margin="5" IsChecked="{Binding IsActive}"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="15" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SaveButton" Content="Сохранить" 
                    Background="#FF4CAF50" Foreground="White"
                    Click="SaveButton_Click"/>
            <Button Content="Отмена" 
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>