<Window x:Class="Lab8.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab8"
        mc:Ignorable="d"
        Title="Бытовое обслуживание населения" 
        Height="600" Width="740"
        WindowStartupLocation="CenterScreen" Closed="Window_Closed">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="8 4"/>
            <Setter Property="MinWidth" Value="80"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#FF2D5F91" Padding="10">
            <TextBlock Text="БЫТОВОЕ ОБСЛУЖИВАНИЕ НАСЕЛЕНИЯ" 
                       Foreground="White" 
                       FontSize="20" 
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- Панель поиска -->
        <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="0 0 0 1" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Поиск:" FontWeight="Bold"/>
                <TextBox Grid.Column="1" x:Name="SearchTextBox" 
                         TextChanged="SearchTextBox_TextChanged"
                         Padding="5">
                    <TextBox.ToolTip>
                        <ToolTip>
                            Поиск по названию, категории или поставщику
                        </ToolTip>
                    </TextBox.ToolTip>
                </TextBox>

                <ComboBox Grid.Column="2" x:Name="CategoryFilterComboBox" 
                          Margin="10 5 5 5" Width="150"
                          SelectionChanged="CategoryFilterComboBox_SelectionChanged">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <Button Grid.Column="3" Content="Сбросить фильтры" 
                        Click="ResetFiltersButton_Click"/>
            </Grid>
        </Border>

        <!-- Таблица с услугами -->
        <DataGrid Grid.Row="2" x:Name="ServicesDataGrid" ItemsSource="{Binding}" SelectionMode="Extended" SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding Category}" Width="120"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat=N2}" Width="80"/>
                <DataGridTextColumn Header="Поставщик" Binding="{Binding Provider}" Width="120"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="110"/>
                <DataGridCheckBoxColumn Header="Активна" Binding="{Binding IsActive}" Width="60"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- Панель кнопок -->
        <Border Grid.Row="3" Background="#FFF0F0F0" Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="AddButton" Content="Добавить" 
                        Background="#FF4CAF50" Foreground="White"
                        Click="AddButton_Click"/>
                <Button x:Name="EditButton" Content="Изменить" 
                        Background="#FF2196F3" Foreground="White"
                        Click="EditButton_Click"/>
                <Button x:Name="DeleteButton" Content="Удалить" 
                        Background="#FFF44336" Foreground="White"
                        Click="DeleteButton_Click"/>
                <Button x:Name="RefreshButton" Content="Обновить" 
                        Background="#FF9E9E9E" Foreground="White"
                        Click="RefreshButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
