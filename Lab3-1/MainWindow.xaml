<Window x:Class="Lab3_1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab3_1"
        mc:Ignorable="d"
       Title="Водительская лицензия" Height="550" Width="800">
    <Window.Resources>
        <local:EnumGenderConverter x:Key="genderConverter"/>
        <local:ImageConverter x:Key="imageConverter"/>
        <Style TargetType="TextBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip" 
                            Value="{Binding RelativeSource={RelativeSource Self}, 
                            Path=(Validation.Errors)[0].ErrorContent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DatePicker">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip" 
                            Value="{Binding RelativeSource={RelativeSource Self}, 
                            Path=(Validation.Errors)[0].ErrorContent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <ControlTemplate x:Key="ErrorTemplate">
            <StackPanel Orientation="Horizontal">
                <Border BorderBrush="Red" BorderThickness="6">
                    <AdornedElementPlaceholder/>
                </Border>
                <TextBlock Text="***" Foreground="Red" FontSize="28">
                </TextBlock>
            </StackPanel>
        </ControlTemplate>
    </Window.Resources>
    <Grid x:Name="grid">
        <Grid.Background>
            <ImageBrush ImageSource="/Images/background.jpg"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Label Content="Водительская лицензия" Grid.Column="1" Grid.ColumnSpan="2" 
               HorizontalAlignment="Center" FontSize="36" FontFamily="Times New Roman" 
               Foreground="Black"/>
        <Image x:Name="image" Source="{Binding UriImage, Converter={StaticResource imageConverter}}" MouseDown="Image_MouseDown" Grid.RowSpan="6" Margin="20"/>
        <TextBox x:Name="textBoxName" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" 
                 FontSize="16" Margin="12" FontWeight="Bold" 
                 Validation.ErrorTemplate="{StaticResource ErrorTemplate}">
            <TextBox.Text>
                <Binding Path="Name">
                    <Binding.ValidationRules>
                        <DataErrorValidationRule/>
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>
        <TextBox x:Name="textBoxNumber" Grid.Row="1" Grid.Column="2"  
                 VerticalAlignment="Center" FontSize="16" Margin="12" 
                 Validation.ErrorTemplate="{StaticResource ErrorTemplate}" FontWeight="Bold">
            <TextBox.Text>
                <Binding Path="Number">
                    <Binding.ValidationRules>
                        <DataErrorValidationRule/>
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>
        <TextBox x:Name="textBoxAdress" Grid.Row="2" Grid.Column="1"  
                 VerticalAlignment="Center" FontSize="16" Margin="12" FontWeight="Bold"
            Validation.ErrorTemplate="{StaticResource ErrorTemplate}">
            <TextBox.Text>
                <Binding Path="Adress">
                    <Binding.ValidationRules>
                        <DataErrorValidationRule/>
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>
        <StackPanel Grid.Row="2" Grid.Column="2" Orientation="Horizontal">
            <Label Content="CLASS" VerticalAlignment="Center" Margin="10"/>
            <TextBox x:Name="textBoxClass1" VerticalAlignment="Center" Width="40"  FontSize="16" 
                     Margin="30 10 10 10" FontWeight="Bold" Validation.ErrorTemplate="{StaticResource ErrorTemplate}">
                <TextBox.Text>
                    <Binding Path="Class1">
                        <Binding.ValidationRules>
                            <DataErrorValidationRule/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
        </StackPanel>
        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal">
            <Label Content="DOB" VerticalAlignment="Center" Margin="10"/>
            <DatePicker x:Name="datePickerDOB" VerticalAlignment="Center" Width="160"  FontSize="16" 
                     Margin="20 10 10 10" FontWeight="Bold"   
                        Validation.ErrorTemplate="{StaticResource ErrorTemplate}">
                <DatePicker.SelectedDate>
                    <Binding Path="Dob">
                        <Binding.ValidationRules>
                            <DataErrorValidationRule/>
                        </Binding.ValidationRules>
                    </Binding>
                </DatePicker.SelectedDate>
            </DatePicker>
        </StackPanel>
        <StackPanel Grid.Row="3" Grid.Column="2" Orientation="Horizontal">
            <Label Content="ISS" VerticalAlignment="Center" Margin="10"/>
            <DatePicker x:Name="datePickerISS" VerticalAlignment="Center" Width="160"  FontSize="16" 
                     Margin="20 10 10 10" FontWeight="Bold"  
                        Validation.ErrorTemplate="{StaticResource ErrorTemplate}">
                <DatePicker.SelectedDate>
                    <Binding Path="Iss">
                        <Binding.ValidationRules>
                            <DataErrorValidationRule/>
                        </Binding.ValidationRules>
                    </Binding>
                </DatePicker.SelectedDate>
            </DatePicker>
        </StackPanel>
        <StackPanel Grid.Row="4" Grid.Column="2" Orientation="Horizontal">
            <Label Content="EXP" VerticalAlignment="Center" Margin="10"/>
            <DatePicker x:Name="datePickerEXP" VerticalAlignment="Center" Width="160"  FontSize="16" 
                     Margin="20 10 10 10" FontWeight="Bold"                         
                        Validation.ErrorTemplate="{StaticResource ErrorTemplate}">
                <DatePicker.SelectedDate>
                    <Binding Path="Exp">
                        <Binding.ValidationRules>
                            <DataErrorValidationRule/>
                        </Binding.ValidationRules>
                    </Binding>
                </DatePicker.SelectedDate>
            </DatePicker>
        </StackPanel>
        <GroupBox Grid.Row="4" Grid.Column="1" Header="SEX">
            <StackPanel Orientation="Horizontal">
                <RadioButton x:Name="radioButtonM" Content="M" Margin="20 0" FontSize="16" FontWeight="Bold" 
                             IsChecked="{Binding Path=Gender, Converter={StaticResource genderConverter},
                             ConverterParameter={x:Static local:GENDER.male}}"/>
                <RadioButton x:Name="radioButtonF" Content="F" Margin="20 0" FontSize="16" FontWeight="Bold"         
                             IsChecked="{Binding Path=Gender, Converter={StaticResource genderConverter},
                             ConverterParameter={x:Static local:GENDER.female}}"/>
                <RadioButton x:Name="radioButtonO" Content="Other" Margin="20 0" FontSize="16" FontWeight="Bold"
                             IsChecked="{Binding Path=Gender, Converter={StaticResource genderConverter},
                             ConverterParameter={x:Static local:GENDER.other}}"/>
            </StackPanel>
        </GroupBox>
        <StackPanel Grid.Row="5" Grid.Column="1" Orientation="Horizontal">
            <Label Content="Глаза" VerticalAlignment="Center" Margin="10"/>
            <ComboBox x:Name="comboBoxEyes" VerticalAlignment="Center" Width="160"  FontSize="16" 
                     Margin="20 10 10 10" FontWeight="Bold" SelectedValue="{Binding Coloreyes}"/>
        </StackPanel>
        <CheckBox x:Name="checkBoxDonor" Grid.Row="6" Grid.Column="0" Content="Добровольный донор органов" FontSize="14" 
                  Margin="10" VerticalAlignment="Center" IsChecked="{Binding Donor}"/>
        <StackPanel Grid.Row="6" Grid.Column="1" Orientation="Horizontal">
            <Label Content="Рост" VerticalAlignment="Center" Margin="15 0"/>
            <Slider x:Name="sliderHGT" Width="160" VerticalAlignment="Center" TickPlacement="TopLeft" AutoToolTipPlacement="TopLeft" 
                    Minimum="100" Maximum="250" TickFrequency="10" Value="{Binding Hgt}"/>
        </StackPanel>
        <Button Grid.Row="7" Content="Загрузить" Width="160" 
                HorizontalAlignment="Center" FontSize="16" Margin="0,14,0,13" Click="ButtonLoad_Click"/>
        <Button Grid.Row="7" Grid.Column="1" Content="Сохранить" Width="160" 
                HorizontalAlignment="Center" FontSize="16" Margin="0,14,0,13" Click="ButtonSave_Click"/>
        <Button Grid.Row="7" Grid.Column="2" Content="Отчистить" Width="160" Height="40" 
                HorizontalAlignment="Center" FontSize="16" Margin="30 10" Click="ButtonClear_Click"/>

    </Grid>
</Window>
