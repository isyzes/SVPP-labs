<Window x:Class="Lab7_1.StatsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        xmlns:local="clr-namespace:Lab7_1"
        Title="Статистика" 
        Height="400" Width="500"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Статистика услуг" 
                   FontSize="16" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0 0 0 10"/>

        <!-- Статистика -->
        <ListView Grid.Row="1" x:Name="StatsListView">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Показатель" Width="200" DisplayMemberBinding="{Binding Key}"/>
                    <GridViewColumn Header="Значение" Width="250" DisplayMemberBinding="{Binding Value}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>